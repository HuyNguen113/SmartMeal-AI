import { Router } from 'express'; import { homNay, lichSu } from '../controllers/duBaoController.js'; import xacThuc from '../middleware/xacThuc.js'; import phanQuyen from '../middleware/phanQuyen.js'; const router = Router(); router.get('/hom-nay', xacThuc, phanQuyen('nhan_vien', 'quan_tri'), homNay); router.get('/lich-su', xacThuc, phanQuyen('quan_tri'), lichSu); export default router;
